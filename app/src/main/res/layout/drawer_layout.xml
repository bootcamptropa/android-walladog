<?xml version="1.0" encoding="utf-8"?>
<android.support.v4.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/drawer"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true">

  <!-- set here the layout of the Drawer Main Activity -->
  <include
      android:id="@+id/drawer_layout_main_content"
      layout="@layout/drawer_layout_container"/>

  <android.support.design.widget.NavigationView
      android:id="@+id/drawer_layout_navigation_view"
      android:layout_height="match_parent"
      android:layout_width="wrap_content"
      android:layout_gravity="start"
      app:headerLayout="@layout/drawer_header"
      app:menu="@menu/wddrawer_menu"
      app:itemIconTint="@color/primary_color"
      app:itemTextColor="@color/primary_color"
      app:itemBackground="@drawable/wddrawer_item"
      />
</android.support.v4.widget.DrawerLayout>

